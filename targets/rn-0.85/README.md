# RN 0.85.3 + Hermes V1

Stub — filled in by Task 3.8.

`sample85/` is the pristine RN 0.85.3 baseline app (scaffolded via
`@react-native-community/cli@latest init sample85 --version 0.85.3
--skip-install --pm npm`), committed the same way `targets/rn-0.79/sample79`
is: app source in git, `node_modules` gitignored.

RN 0.85.3 ships Hermes V1 `hermes-v250829098.0.10` out of the box
(`sample85/node_modules/react-native/sdks/.hermesv1version`). Later tasks
bump this to `260318099.0.1`.
